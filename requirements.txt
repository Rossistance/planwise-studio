# PlanWise — per-job project controls
fastapi>=0.110
uvicorn>=0.27

# Reads the shared Vista extract (read-only)
openpyxl>=3.1

# Document library: PDF page counts / page extraction; multipart uploads
pypdf>=4.0
python-multipart>=0.0.9

# Tests
pytest>=8.0
httpx>=0.27

# Phase 3b: AI providers (REST) + Outlook companion (COM, Windows only)
pywin32>=306; sys_platform == "win32"
truststore>=0.9

# Phase 5e: web push, so an RFI reply reaches a phone in the field. Pulls in
# py-vapid and cryptography for the VAPID signing the push services require.
pywebpush>=2.0

# Binary .mpp import. MPXJ is a Java library — the only production-grade reader
# for Microsoft Project's proprietary format — so it needs a JVM alongside it;
# scripts/install-jre.sh puts one in the Render build. Both ship Linux and
# Windows wheels, and neither is imported unless somebody actually uploads an
# .mpp, so an environment with no Java installs these harmlessly and simply
# reports that .mpp is unavailable (MSPDI XML and printed PDFs still import).
mpxj>=13.0
JPype1>=1.5
